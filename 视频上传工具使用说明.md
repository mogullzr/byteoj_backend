# 阿里云视频上传工具使用说明

## 📋 概述

已完成阿里云 VOD（视频点播）上传工具的完善，支持前端上传视频文件到阿里云。

## 🎯 主要改进

### 1. **UploadDemo.java** - 核心上传工具类

#### 主要功能：
- ✅ 接收前端 `MultipartFile` 视频文件
- ✅ 自动验证视频格式（支持 mp4, avi, mov, wmv, flv, mkv, webm）
- ✅ 文件大小限制（50MB）
- ✅ 自动生成唯一文件名（UUID）
- ✅ 临时文件管理（上传后自动清理）
- ✅ 完整的异常处理和日志记录
- ✅ 返回 VideoId 供后续播放使用

#### 核心方法：

```java
/**
 * 从MultipartFile上传视频到阿里云VOD
 * @param file 前端上传的视频文件
 * @param title 视频标题（可选，默认为文件名）
 * @return VideoId 上传成功后的视频ID
 */
public String uploadVideoToVod(MultipartFile file, String title)
```

### 2. **VideoUploadController.java** - 视频上传控制器

#### 接口说明：

##### 单文件上传
- **URL**: `POST /api/video/upload`
- **参数**:
  - `file` (必填): 视频文件
  - `title` (可选): 视频标题
- **返回**: VideoId
- **限流**: 60秒内最多10次
- **需要登录**: 是

##### 批量上传
- **URL**: `POST /api/video/upload/batch`
- **参数**:
  - `files` (必填): 多个视频文件数组
- **返回**: VideoId 数组
- **限流**: 300秒内最多5次
- **需要登录**: 是

## 🚀 使用方法

### 前端调用示例（JavaScript/Fetch）

#### 单文件上传：
```javascript
const formData = new FormData();
formData.append('file', videoFile); // videoFile 是 File 对象
formData.append('title', '我的视频标题'); // 可选

fetch('/api/video/upload', {
  method: 'POST',
  headers: {
    // 如果需要认证，添加 token
    'Authorization': 'Bearer YOUR_TOKEN'
  },
  body: formData
})
.then(response => response.json())
.then(data => {
  if (data.code === 0) {
    const videoId = data.data; // 获取 VideoId
    console.log('上传成功，VideoId:', videoId);
    // 可以使用 videoId 获取播放凭证
  } else {
    console.error('上传失败:', data.message);
  }
})
.catch(error => {
  console.error('上传异常:', error);
});
```

#### 批量上传：
```javascript
const formData = new FormData();
videoFiles.forEach(file => {
  formData.append('files', file);
});

fetch('/api/video/upload/batch', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_TOKEN'
  },
  body: formData
})
.then(response => response.json())
.then(data => {
  if (data.code === 0) {
    const videoIds = data.data; // 获取 VideoId 数组
    console.log('批量上传成功，VideoIds:', videoIds);
  }
});
```

### 前端调用示例（Vue + Axios）

```javascript
import axios from 'axios';

// 单文件上传
async function uploadVideo(file, title) {
  const formData = new FormData();
  formData.append('file', file);
  if (title) {
    formData.append('title', title);
  }

  try {
    const response = await axios.post('/api/video/upload', formData, {
      headers: {
        'Content-Type': 'multipart/form-data'
      },
      timeout: 300000 // 5分钟超时
    });
    
    if (response.data.code === 0) {
      return response.data.data; // VideoId
    }
  } catch (error) {
    console.error('上传失败:', error);
    throw error;
  }
}
```

### 后端 Service 层使用示例

如果你需要在 Service 层直接调用工具类：

```java
@Service
public class CourseService {
    
    @Autowired
    private UploadDemo uploadDemo;
    
    public String uploadCourseVideo(MultipartFile videoFile, String title) {
        // 直接调用上传方法
        String videoId = uploadDemo.uploadVideoToVod(videoFile, title);
        
        // 可以将 videoId 保存到数据库
        // ...
        
        return videoId;
    }
}
```

## 📝 配置说明

确保 `application.yml` 中已配置阿里云 VOD 相关参数：

```yaml
aliyun:
  vod:
    accessKeyId: ${hm.aliyun.vod.accessKeyId}
    accessKeySecret: ${hm.aliyun.vod.accessKeySecret}
```

## ⚠️ 注意事项

1. **文件大小限制**：
   - 当前限制为 50MB
   - 如需修改，在 `UploadDemo.java` 第 126 行调整
   - 同时需要确保 `application.yml` 中的 `max-file-size` 配置匹配

2. **支持的格式**：
   - mp4, avi, mov, wmv, flv, mkv, webm
   - 如需添加其他格式，修改 `UploadDemo.java` 第 111 行的 `allowedExtensions` 数组

3. **临时文件清理**：
   - 上传完成后会自动删除临时文件
   - 如果删除失败会在日志中记录警告

4. **超时设置**：
   - VOD 客户端超时：10秒
   - 建议前端设置合理的超时时间（如 5 分钟）

5. **安全性**：
   - 接口已添加限流保护
   - 需要登录认证
   - 文件类型和大小都有严格校验

## 🔧 后续可扩展功能

1. **获取播放凭证**：
   - 可使用 `VodUtils.getPayAuth(videoId)` 获取播放凭证
   - 前端使用播放凭证播放视频

2. **上传进度**：
   - 当前是同步上传，如需进度显示可改用异步 + WebSocket

3. **分片上传**：
   - 大文件可改用阿里云分片上传 API

4. **视频信息存储**：
   - 建议将 VideoId 与业务数据关联存储到数据库

## 📊 日志示例

```
2024-01-01 12:00:00 [INFO] 临时文件创建成功: /tmp/upload_123456.mp4
2024-01-01 12:00:01 [INFO] 获取到VideoId: abc123def456
2024-01-01 12:00:10 [INFO] 视频上传成功, VideoId: abc123def456, 文件名: test.mp4
2024-01-01 12:00:10 [INFO] 临时文件已删除: /tmp/upload_123456.mp4
```

## 🎉 完成

工具已完善并可直接使用！如有问题请查看日志或联系开发团队。

