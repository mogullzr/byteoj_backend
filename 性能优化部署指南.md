# 🚀 性能优化部署指南

## 📋 本次修复内容

### 1. ✅ 修复类型不匹配问题
- 修复 `idx` 字段类型：`Long` → `String`（对应数据库 `varchar(256)`）
- 修复 `timePenalty` 字段类型：`long` → `int`（对应数据库 `int`）
- **影响**：避免类型转换开销，确保索引有效使用

### 2. ✅ 降低 RabbitMQ 并发消费者数量
**修改前**：
```yaml
concurrency: 20
max-concurrency: 50  # ❌ 最多50个消费者同时判题！
```

**修改后**：
```yaml
concurrency: 2
max-concurrency: 4   # ✅ 最多4个消费者（判题是CPU密集型）
```

**预期效果**：CPU 使用率从 100% 降低到 40-60%

### 3. ✅ 增加数据库连接池
```yaml
maximum-pool-size: 10 → 20  # 翻倍
```

### 4. ✅ 添加数据库索引
- `competitions_problems_algorithm`: `idx_competition_idx`
- `competition_ac_problems_algorithm`: `idx_competition_user_idx`
- `competitions_user`: `idx_competition_user`

---

## 🛠️ 部署步骤（按顺序执行）

### 步骤1：备份数据库（必须！）
```bash
# 连接到服务器
ssh root@your-server

# 备份数据库
docker exec mysql mysqldump -u root -p142398749lzr byteoj > /root/backup/byteoj_backup_$(date +%Y%m%d_%H%M%S).sql

# 确认备份成功
ls -lh /root/backup/
```

### 步骤2：添加数据库索引
```bash
# 上传 SQL 文件
scp sql/add_performance_indexes.sql root@your-server:/root/

# 执行 SQL
docker exec -i mysql mysql -u root -p142398749lzr < /root/add_performance_indexes.sql

# 或者手动执行
docker exec -it mysql mysql -u root -p142398749lzr
# 然后复制粘贴 SQL 内容
```

### 步骤3：更新代码
```bash
# 在本地编译
mvn clean package -DskipTests

# 上传 JAR 到服务器
scp target/backend-0.0.1-SNAPSHOT.jar root@your-server:/root/opt/

# 或使用 Git 拉取最新代码后重新构建
cd /root/opt
git pull
docker-compose build byteoj
```

### 步骤4：重启服务
```bash
# 停止服务
docker-compose down

# 启动服务
docker-compose up -d

# 查看启动日志
docker logs -f byteoj
```

### 步骤5：验证部署
```bash
# 1. 检查服务是否正常启动
docker ps

# 2. 检查应用日志
docker logs byteoj | tail -100

# 3. 检查 RabbitMQ 连接
docker logs byteoj | grep "RabbitMQ"

# 4. 访问健康检查（如果有）
curl http://localhost:7091/api/health

# 5. 提交一个测试判题任务
# ...
```

---

## 📊 性能监控

### 监控系统负载
```bash
# 实时监控（每2秒更新一次）
watch -n 2 'echo "=== CPU & 负载 ==="; top -bn1 | head -5; echo "=== RabbitMQ 队列 ==="; docker exec rabbitmq rabbitmqctl list_queues name messages consumers'
```

### 监控应用日志
```bash
# 监控判题消费者日志
docker logs -f byteoj | grep "判题消费者"

# 监控原子更新日志
docker logs -f byteoj | grep "原子更新"
```

### 监控数据库性能
```bash
# 连接数据库
docker exec -it mysql mysql -u root -p142398749lzr

# 查看慢查询
SHOW VARIABLES LIKE 'slow_query%';
SHOW STATUS LIKE 'Slow_queries';

# 查看连接数
SHOW STATUS LIKE 'Threads_connected';
SHOW STATUS LIKE 'Max_used_connections';

# 查看索引使用情况
EXPLAIN SELECT * FROM competitions_problems_algorithm WHERE competition_id = 1 AND idx = 'A';
```

---

## ✅ 验证优化效果

### 预期效果
| 指标 | 优化前 | 优化后（预期） |
|------|--------|--------------|
| CPU 使用率 | 100% | 40-60% |
| 系统负载 | 200% | 80-120% |
| 并发消费者 | 20-50个 | 2-4个 |
| 数据库连接池 | 10个 | 20个 |
| 响应速度 | 慢 | 快（有索引） |

### 测试方法
```bash
# 使用 Apache Bench 压测
ab -n 1000 -c 15 -p submit.json -T 'application/json' http://your-domain/api/algorithm/judge/submit

# 或使用 JMeter
# - 100个线程
# - 每秒15次请求
# - 持续1分钟
```

---

## 🚨 问题排查

### 问题1：服务启动失败
```bash
# 查看详细日志
docker logs byteoj

# 检查配置
docker exec byteoj cat /app/config/application.yml

# 检查端口占用
netstat -tlnp | grep 7091
```

### 问题2：RabbitMQ 连接失败
```bash
# 检查 RabbitMQ 状态
docker logs rabbitmq

# 检查用户权限
docker exec rabbitmq rabbitmqctl list_users
docker exec rabbitmq rabbitmqctl list_permissions

# 重置 RabbitMQ
docker-compose restart rabbitmq
```

### 问题3：数据库连接失败
```bash
# 检查 MySQL 状态
docker logs mysql

# 测试连接
docker exec mysql mysql -u root -p142398749lzr -e "SHOW DATABASES;"

# 检查权限
docker exec mysql mysql -u root -p142398749lzr -e "SHOW GRANTS FOR 'root'@'%';"
```

### 问题4：CPU 仍然很高
```bash
# 检查 RabbitMQ 消费者数量
docker logs byteoj | grep "concurrentConsumers"

# 如果 CPU 仍然100%，进一步降低并发：
# 修改 application.yml:
# concurrency: 1
# max-concurrency: 2

# 或者考虑升级服务器配置
```

---

## 🎯 下一步优化（可选）

### 1. 使用 Redis 缓存
- 缓存题目信息
- 缓存用户信息
- 减少数据库查询

### 2. 分布式判题
- 将判题服务独立出来
- 使用多台判题机
- 水平扩展判题能力

### 3. 代码预编译
- 对常用语言模板预编译
- 减少重复编译开销

### 4. 使用 CDN
- 静态资源使用 CDN
- 减轻服务器压力

---

## 📞 联系支持

如果优化后仍有问题：
1. 收集日志：`docker logs byteoj > app.log`
2. 收集监控数据：截图 CPU、内存、负载
3. 提供服务器配置信息

---

## ✅ 部署检查清单

- [ ] 1. 备份数据库
- [ ] 2. 添加数据库索引
- [ ] 3. 更新代码
- [ ] 4. 重启服务
- [ ] 5. 检查服务启动
- [ ] 6. 提交测试判题
- [ ] 7. 监控系统负载（观察10分钟）
- [ ] 8. 验证数据准确性
- [ ] 9. 记录优化效果
- [ ] 10. 保存备份和日志

---

**祝部署顺利！🚀**

