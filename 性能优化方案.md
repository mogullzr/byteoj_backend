# 🚀 高并发性能优化方案

## 🔴 当前问题
- **CPU使用率**: 100%
- **系统负载**: 200%（4核机器，意味着严重过载）
- **测试条件**: 每秒15次请求

## 📊 问题分析

### 1. 判题本身就是CPU密集型操作
- 编译代码（gcc/javac）- 极耗CPU
- 执行代码（沙箱运行）- 极耗CPU
- 多组测试用例 - 成倍耗CPU

**每秒15次判题 = 每秒编译+运行15次代码 = CPU必然爆满！**

### 2. RabbitMQ消费者过多
```java
setConcurrentConsumers(5);
setMaxConcurrentConsumers(10);
```
- 10个消费者同时判题 = 10个编译器+10个代码执行 = CPU崩溃

## ✅ 解决方案

### 方案1：降低并发消费者数量（立即生效）

根据您的服务器配置，建议：
- **2核心机器**: `concurrentConsumers(1)`, `maxConcurrentConsumers(2)`
- **4核心机器**: `concurrentConsumers(2)`, `maxConcurrentConsumers(4)`
- **8核心机器**: `concurrentConsumers(4)`, `maxConcurrentConsumers(6)`

**计算公式**: `maxConcurrentConsumers = CPU核心数 * 0.5 ~ 0.75`

### 方案2：增加数据库连接池（避免连接等待）

```yaml
# application.yml
spring:
  datasource:
    hikari:
      maximum-pool-size: 20          # 增加到20（原来可能只有10）
      minimum-idle: 5                # 最小空闲连接
      connection-timeout: 30000      # 连接超时30秒
      idle-timeout: 600000           # 空闲连接10分钟后释放
      max-lifetime: 1800000          # 连接最大存活30分钟
      connection-test-query: SELECT 1
```

### 方案3：为高频查询的字段添加索引

```sql
-- 为 problem_algorithm_bank 添加索引
ALTER TABLE problem_algorithm_bank ADD INDEX idx_problem_id (problem_id);

-- 为 competitions_problems_algorithm 添加复合索引
ALTER TABLE competitions_problems_algorithm 
ADD INDEX idx_competition_idx (competition_id, idx);

-- 为 competition_ac_problems_algorithm 添加复合索引
ALTER TABLE competition_ac_problems_algorithm 
ADD INDEX idx_competition_user_idx (competition_id, uuid, idx);

-- 为 competitions_user 添加复合索引
ALTER TABLE competitions_user 
ADD INDEX idx_competition_user (competition_id, uuid);
```

### 方案4：优化日志输出（减少IO开销）

```yaml
# application.yml
logging:
  level:
    root: WARN                          # 全局日志级别改为WARN
    com.example.backend: INFO           # 业务代码INFO
    org.springframework.amqp: WARN      # RabbitMQ日志降级
    org.hibernate.SQL: ERROR            # SQL日志关闭（生产环境）
  pattern:
    console: "%d{HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"  # 简化日志格式
```

### 方案5：判题过程异步化（关键优化）

判题是最耗CPU的操作，需要：
1. 使用独立的线程池处理编译和执行
2. 限制同时编译的数量
3. 考虑使用外部判题机（分布式判题）

```java
// 添加判题线程池配置
@Configuration
public class JudgeExecutorConfig {
    
    @Bean("judgeExecutor")
    public ThreadPoolExecutor judgeExecutor() {
        return new ThreadPoolExecutor(
            2,                              // 核心线程数（根据CPU核心数调整）
            4,                              // 最大线程数
            60L, TimeUnit.SECONDS,          // 空闲线程存活时间
            new LinkedBlockingQueue<>(200), // 队列容量
            new ThreadFactoryBuilder()
                .setNameFormat("judge-pool-%d")
                .build(),
            new ThreadPoolExecutor.CallerRunsPolicy()  // 拒绝策略
        );
    }
}
```

### 方案6：启用 Redis 缓存（减少数据库压力）

```yaml
# application.yml
spring:
  redis:
    host: redis
    port: 6379
    database: 0
    password: "Redis的密码"
    timeout: 3000
    lettuce:
      pool:
        max-active: 20
        max-idle: 10
        min-idle: 5
        max-wait: 3000
```

缓存题目信息、用户信息等高频读取数据。

### 方案7：启用 JVM 性能优化

```dockerfile
# 在 Dockerfile 或启动脚本中添加 JVM 参数
ENV JAVA_OPTS="-Xms2g -Xmx2g -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -XX:+HeapDumpOnOutOfMemoryError"
```

---

## 🛠️ 立即实施的优化（按优先级）

### 优先级1：调整 RabbitMQ 消费者数量（最重要）

修改 `RabbitMQConfig.java`：

```java
@Bean
public SimpleRabbitListenerContainerFactory rabbitListenerContainerFactory(
        ConnectionFactory connectionFactory) {
    SimpleRabbitListenerContainerFactory factory = new SimpleRabbitListenerContainerFactory();
    factory.setConnectionFactory(connectionFactory);
    factory.setMessageConverter(jsonMessageConverter());

    // 🔥 关键修改：根据CPU核心数调整
    // 2核: 1-2, 4核: 2-4, 8核: 4-6
    factory.setConcurrentConsumers(2);              // 降低到2
    factory.setMaxConcurrentConsumers(4);           // 降低到4
    factory.setPrefetchCount(1);                    // 保持1
    factory.setAcknowledgeMode(org.springframework.amqp.core.AcknowledgeMode.MANUAL);
    factory.setDefaultRequeueRejected(false);

    return factory;
}
```

### 优先级2：增加数据库连接池

修改 `application.yml`：

```yaml
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/byteoj?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: ${MYSQL_PASSWORD:142398749lzr}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
```

### 优先级3：添加数据库索引

执行以下SQL：

```sql
-- 连接数据库
USE byteoj;

-- 检查现有索引
SHOW INDEX FROM problem_algorithm_bank;
SHOW INDEX FROM competitions_problems_algorithm;
SHOW INDEX FROM competition_ac_problems_algorithm;
SHOW INDEX FROM competitions_user;

-- 添加缺失的索引（如果不存在）
ALTER TABLE competitions_problems_algorithm 
ADD INDEX idx_competition_idx (competition_id, idx);

ALTER TABLE competition_ac_problems_algorithm 
ADD INDEX idx_competition_user_idx (competition_id, uuid, idx);

ALTER TABLE competitions_user 
ADD INDEX idx_competition_user (competition_id, uuid);
```

### 优先级4：优化日志级别

修改 `application.yml`：

```yaml
logging:
  level:
    root: WARN
    com.example.backend: INFO
    org.springframework.amqp: WARN
    org.hibernate: ERROR
```

---

## 📈 性能测试对比

### 优化前
- CPU: 100%
- 负载: 200%
- 每秒处理: 15次请求，系统崩溃

### 优化后（预期）
- CPU: 40-60%
- 负载: 80-120%
- 每秒处理: 15次请求，系统稳定

---

## 🎯 长期解决方案

### 1. 分布式判题架构
```
前端 -> API Server -> RabbitMQ -> 多个判题机
                          |
                          ├─> Judge Server 1 (2核)
                          ├─> Judge Server 2 (2核)
                          └─> Judge Server 3 (2核)
```

### 2. 使用成熟的判题系统
- **Judge0**: https://github.com/judge0/judge0
- **OnlineJudge**: https://github.com/QingdaoU/OnlineJudge
- 这些系统已经高度优化，支持大规模并发

### 3. 代码预编译
- 对于常用语言模板，提前编译
- 减少重复编译开销

---

## ⚠️ 重要提醒

**判题是极耗资源的操作！**

如果您的需求是：
- **100个用户同时提交**
- **每秒15次请求**

建议的服务器配置：
- **CPU**: 至少8核（16核更好）
- **内存**: 至少16GB
- **或使用分布式架构**: 3-5台判题服务器

**当前4核服务器**处理每秒15次判题请求**接近极限**，这是正常现象！

---

## 🔧 快速修复脚本

创建 `optimize.sh`：

```bash
#!/bin/bash

echo "正在优化系统性能..."

# 1. 添加数据库索引
mysql -u root -p142398749lzr byteoj <<EOF
ALTER TABLE competitions_problems_algorithm ADD INDEX idx_competition_idx (competition_id, idx);
ALTER TABLE competition_ac_problems_algorithm ADD INDEX idx_competition_user_idx (competition_id, uuid, idx);
ALTER TABLE competitions_user ADD INDEX idx_competition_user (competition_id, uuid);
EOF

echo "✅ 数据库索引添加完成"

# 2. 重启服务应用新配置
docker-compose restart byteoj

echo "✅ 服务已重启"
echo "🎉 优化完成！请监控系统负载"
```

运行：
```bash
chmod +x optimize.sh
./optimize.sh
```

---

## 📞 如果问题仍然存在

1. 检查您的服务器CPU核心数：
```bash
lscpu | grep "^CPU(s):"
```

2. 监控实时负载：
```bash
top -H  # 按线程查看
```

3. 查看哪个进程占用CPU：
```bash
ps aux | grep java
```

4. 考虑升级服务器配置或使用分布式架构。

