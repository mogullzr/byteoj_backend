# 🧪 如何验证并发修复是否有效

## 方法一：运行单元测试（推荐）

### 1. 准备测试数据
在数据库中确保存在测试题目（或创建一个）：
```sql
-- 查看是否有题目
SELECT problem_id, test_total, ac_total FROM problem_algorithm_bank LIMIT 1;

-- 如果没有，插入一个测试题目
INSERT INTO problem_algorithm_bank (problem_id, short_name, test_total, ac_total) 
VALUES (1001, 'test', 0, 0);
```

### 2. 修改测试代码中的题目ID
打开 `src/test/java/com/example/backend/ConcurrencyTest.java`，将 `testProblemId` 改为你的真实题目ID：
```java
Long testProblemId = 1001L;  // 改成你的题目ID
```

### 3. 运行测试
```bash
# 运行所有并发测试
mvn test -Dtest=ConcurrencyTest

# 或只运行特定测试
mvn test -Dtest=ConcurrencyTest#testConcurrentIncrementTestTotal
```

### 4. 查看测试结果
✅ **测试通过**：说明并发安全，无丢失更新
```
✅ 测试通过！无丢失更新，并发安全性验证成功！
```

❌ **测试失败**：说明仍存在并发问题
```
expected: <100> but was: <95>  // 期望100，实际只有95，丢失了5次更新
```

---

## 方法二：手动压测（真实环境）

### 1. 使用 JMeter 压测
创建 HTTP 请求配置：
- URL: `http://your-domain/api/algorithm/judge/submit`
- 方法: POST
- 线程数: 100
- 循环次数: 1
- Body: 
```json
{
  "problem_id": 1001,
  "language": "java",
  "source_code": "your test code"
}
```

### 2. 使用 Postman
1. 创建判题提交请求
2. 使用 "Runner" 功能
3. 设置 Iterations: 100
4. 运行并观察结果

### 3. 检查数据库
```sql
-- 提交前记录初始值
SELECT problem_id, test_total, ac_total FROM problem_algorithm_bank WHERE problem_id = 1001;
-- 假设 test_total = 500

-- 执行100次提交后再查询
SELECT problem_id, test_total, ac_total FROM problem_algorithm_bank WHERE problem_id = 1001;
-- 应该是 test_total = 600（正好增加100）
-- 如果是 test_total = 590，说明丢失了10次更新！
```

---

## 方法三：监控日志

### 1. 添加日志（可选）
在 `ProblemAlgorithmServiceImpl.java` 的原子更新处添加日志：
```java
log.info("[原子更新] 题目ID: {}, 增加test_total: 1", problemId);
int rows = problemAlgorithmBankMapper.incrementTestTotal(problemId, 1);
log.info("[原子更新] 影响行数: {}", rows);
```

### 2. 查看实时日志
```bash
# Docker环境
docker logs -f byteoj | grep "原子更新"

# 非Docker环境
tail -f log/spring.log | grep "原子更新"
```

### 3. 统计更新次数
```bash
# 统计日志中的更新次数
docker logs byteoj | grep "原子更新" | grep "题目ID: 1001" | wc -l
```

---

## 方法四：简单的Shell脚本测试

创建 `test_concurrency.sh`：
```bash
#!/bin/bash

# 配置
API_URL="http://localhost:7091/api/algorithm/judge/submit"
PROBLEM_ID=1001
CONCURRENT=100

# 记录初始值
echo "开始测试，提交 $CONCURRENT 个并发请求..."

# 并发提交
for i in $(seq 1 $CONCURRENT); do
  curl -X POST "$API_URL" \
    -H "Content-Type: application/json" \
    -d "{\"problem_id\": $PROBLEM_ID, \"language\": \"java\", \"source_code\": \"test$i\"}" \
    &> /dev/null &
done

# 等待所有请求完成
wait

echo "所有请求已完成，请检查数据库中的 test_total 是否增加了 $CONCURRENT"
```

运行：
```bash
chmod +x test_concurrency.sh
./test_concurrency.sh
```

---

## 期望结果

### ✅ 修复成功的标志
1. **单元测试全部通过**
2. **数据库统计准确**：100次提交，test_total 正好增加100
3. **无异常日志**：没有死锁、超时等错误
4. **性能良好**：100个并发请求在合理时间内完成

### ❌ 仍有问题的表现
1. **测试失败**：期望100，实际小于100
2. **数据不一致**：多次测试结果不同
3. **日志报错**：死锁、超时等错误
4. **性能下降**：请求响应时间显著增加

---

## 常见问题

### Q: 测试时报 "Table doesn't exist"
**A**: 确保数据库中存在相应的表，检查 `application.yml` 中的数据库配置。

### Q: 测试总是失败，怎么办？
**A**: 
1. 检查是否还有其他地方使用了非原子更新
2. 检查是否有代码在原子更新后又调用了 `update()` 覆盖数据
3. 查看日志中的详细错误信息

### Q: 如何恢复测试数据？
**A**:
```sql
-- 重置测试题目的统计数据
UPDATE problem_algorithm_bank 
SET test_total = 0, ac_total = 0 
WHERE problem_id = 1001;
```

---

## 下一步

修复验证成功后：
1. ✅ 部署到生产环境
2. ✅ 监控生产环境数据准确性
3. ✅ 定期检查数据库统计数据
4. ✅ 建立告警机制（如数据异常时发送通知）

---

**注意**：测试时建议先在**测试环境**验证，确认无误后再部署到生产环境！

